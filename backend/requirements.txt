# ---- Web / API ----
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9
email-validator==2.2.0

# ---- Auth ----
python-jose[cryptography]==3.3.0
bcrypt==4.1.3

# ---- Database ----
SQLAlchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9

# ---- Messaging (optional at runtime; falls back to in-memory bus) ----
kafka-python==2.0.2

# ---- Vector memory (optional at runtime; falls back to in-memory store) ----
chromadb==0.5.3
sentence-transformers==3.0.1

# ---- Orchestration (optional at runtime; falls back to sequential engine) ----
langgraph==0.1.5

# ---- AI agents (optional; deterministic heuristics used when no API key) ----
anthropic==0.30.1

# ---- Utilities ----
numpy==1.26.4
httpx==0.27.0
tenacity==8.4.2
python-dotenv==1.0.1
